import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Casos de éxito | FUTURA — Resultados reales en Guatemala",
  description: "Empresas guatemaltecas que digitalizaron su operación con FUTURA. Casos reales con números concretos.",
}

export default function CasosLayout({ children }: { children: React.ReactNode }) {
  return children
}
